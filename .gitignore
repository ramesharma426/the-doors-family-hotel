# Node
node_modules/

# Build artifacts — the GitHub Actions workflow (.github/workflows/deploy.yml)
# runs `npm run build:static` fresh on every push, so docs/ doesn't need to be committed.
dist/
docs/
*.tsbuildinfo

# OS / editor
.DS_Store
Thumbs.db
.vscode/
*.swp
*.swo
*~

# Logs
*.log

# Env files
.env
.env.local

# Test coverage
coverage/
